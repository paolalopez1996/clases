// calcularAreaTriangulo(base, altura)
// Crear una función calcularAreaTriangulo que 
// tome como argumentos la base y la altura de un triángulo y devuelva el área del mismo

function calcularAreaTriangulo (base = 3, altura = 4){

return   base * altura / 2;


}
console.log(calcularAreaTriangulo(3,4))